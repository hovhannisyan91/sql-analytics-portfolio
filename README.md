# SQL Module 

## Building Dummy Database for Testing

This module provides SQL scripts to create and populate a dummy database for testing purposes. The database schema includes tables for users, products, and orders, along with sample data to facilitate testing of SQL queries and database operations.

```sql
SELECT * FROM users;
```
```python
for i in range(10):
    print(i)
```